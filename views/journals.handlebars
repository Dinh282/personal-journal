<h1 class='text-center mt-5'>{{journalPageMessage}}</h1>

{{! <p class="text-center mt-5">We'll have a Create New Jounral button for the user
    to click on to create a new journal. When they click on it, it should take them to
    /journals/new which would render a form to fill out for their journal title and description.
    The HTML code for this page goes inside journals.handlebars. We should also include
     &lt;script&gt; src="/js/script.js" async&gt;&lt;/script&lt; to link a script file to handle 
     requesting for the list of available journals data so that we can inject it into the template(journals.handlebars).
</p> }}

<div class='text-center'>
  <button class='btn btn-primary btn-lg w-25 mt-4 p-3'>
    <a href='/journals/new' style='color: inherit; text-decoration: none;'>
      Create New Journal
    </a>
  </button>
</div>

{{!-- {{#if journals.length}}
  <ul id='journal-list'>
    {{#each journals}}
      <li>
        <a
          href='/journals/view/{{this.id}}'
          class='btn d-flex items-center justify-between'
        >
          <span class='m-2 fw-bold'>{{this.title}}</span>
          <span class='m-2 fw-light'>{{this.content}}</span>
          <span class='text-sm m-2 fw-light'>{{format_date createdAt}}</span>
        </a>
      </li>

    {{/each}}
  </ul>
{{else}}
  <h5 class='text-2xl text-center pt-8 text-stone-600'>You haven't created any
    Journals yet. Click the Create New Journal button to start one.</h5>
{{/if}} --}}

<div class='row'>
  {{#if journals.length}}
    <section id='journal-list'>
      {{#each journals}}
        <div class='col-lg-2 col-md-4 col-sm-6 mb-4'>
          <div class='card'>
            <img class='img-fluid' src='../public/images/notebookBG.svg' />
          </div>
        </div>
      {{/each}}
    </section>
  {{else}}
    <h5 class='text-2xl text-center pt-8 text-stone-600'>You haven't created any
      Journals yet. Click the Create New Journal button to start one.</h5>
  {{/if}}

</div>